{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run current file",
            "type": "process",
            "command": "${workspaceFolder}/.venv/bin/python3",
            "args": [
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}